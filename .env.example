# Copy to .env and fill in values
# Never commit the real .env file

# ── Database ──────────────────────────────────────────────────
DB_PASSWORD=change-me-strong-password

# ── External API keys ─────────────────────────────────────────
FRED_API_KEY=your_fred_key         # https://fred.stlouisfed.org/docs/api/api_key.html
BEA_API_KEY=your_bea_key           # https://apps.bea.gov/API/signup/
CENSUS_API_KEY=your_census_key     # https://api.census.gov/data/key_signup.html
RAPIDAPI_KEY=your_rapidapi_key     # for live listings (optional — falls back to mock)

# ── Email (Resend) ────────────────────────────────────────────
RESEND_API_KEY=                    # https://resend.com/api-keys — blank = emails logged, not sent
EMAIL_FROM="Touse <onboarding@resend.dev>"   # quotes required — use a verified domain in production
FRONTEND_URL=http://localhost:5173 # base URL used to build verification links

# ── Mapbox ────────────────────────────────────────────────────
MAPBOX_TOKEN=your_mapbox_public_token

# ── Deployment ────────────────────────────────────────────────
# For IP-only deployments, set this to e.g. http://1.2.3.4 (no trailing slash).
# For a domain, set to https://yourdomain.com.
# VITE_API_URL is left blank so the frontend uses relative /api/... URLs (recommended).
VITE_API_URL=
# Comma-separated list of extra origins to allow in CORS (e.g. http://1.2.3.4)
EXTRA_CORS_ORIGINS=

# ── Observability ─────────────────────────────────────────────
SENTRY_DSN=                        # leave blank to disable
