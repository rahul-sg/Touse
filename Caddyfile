touse.app {
    # Frontend
    handle /* {
        reverse_proxy frontend:3000
    }

    # API
    handle /api/* {
        reverse_proxy backend:8000
    }

    # Health check
    handle /health {
        reverse_proxy backend:8000
    }

    encode gzip
    log {
        output stdout
    }
}
